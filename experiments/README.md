# Experiments

Each experiment should use a unique ID and store a compact record of configuration, system information, Git state, command line, metrics, and artifact references.

Large generated files should live under `outputs/` or external artifact storage, not be committed here.
