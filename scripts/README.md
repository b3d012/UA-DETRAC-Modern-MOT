# Scripts

M1 should add thin CLI entry points here for dataset download/setup, validation, visualization, split generation, manifest generation, and system-info capture.

Keep reusable logic in `src/`; scripts should primarily parse arguments and call library functions.
