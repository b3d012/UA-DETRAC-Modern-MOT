experiment:
  name: m1_foundation
  stage: benchmark_foundation
  seed: 42
  uses_test_for_tuning: false

dataset:
  config: configs/dataset/ua_detrac.yaml
  validation:
    fraction: 0.20
    split_unit: sequence
    stratification: null
    note: "Choose/finalize a deterministic train/val strategy using official-train metadata only."
